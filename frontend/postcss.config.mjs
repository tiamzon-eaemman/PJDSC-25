const config = {
  // Use the Tailwind v4 PostCSS plugin package name so Next.js's builder
  // loads the expected plugin implementation.
  plugins: ["@tailwindcss/postcss", "autoprefixer"],
};

export default config;
