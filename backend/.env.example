# Example DATABASE_URL (Postgres) for Supabase
# This must be the full Postgres connection string, not the https project url.
# Example:
# DATABASE_URL=postgresql://postgres:password@db-host.supabase.co:5432/postgres

# Replace with your actual Postgres connection string before running the backend
DATABASE_URL=
